import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexGrow: 1,
    gap: 10,
  },
  text: {
    fontSize: 16,
    fontWeight: 'bold',
  },
});

export default styles;
